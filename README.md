Navigation Bar component with a slider which resizes itself to match the size of the text of the selected link.
This application uses serve-npm package.
Do a npm install and then run the command 'serve' to run this on localhost:3000
